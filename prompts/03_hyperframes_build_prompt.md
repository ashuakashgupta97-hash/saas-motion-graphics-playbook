# HyperFrames Build Prompt

```text
Convert this storyboard into an implementation-ready HyperFrames build brief.

Include:
- scene list with timestamps
- HTML/CSS/GSAP motion intent
- asset usage by scene
- transition style
- subtitle behavior
- audio sync notes
- UI sound effect suggestions
- final render target

Assume HyperFrames is the main production surface.
If a scene would be easier as a separate insert or previs, explicitly mark it as optional Remotion support rather than making Remotion the main path.

Do not invent UI that is not needed. Use real screenshots for proof and designed motion for invisible logic.
```
